rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // AlwaysReady Care — authenticated users can read/write their own org data
    match /orgs/{orgId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
